CLAUDE.frontend.md



\# Project: [novel-front]



\## 技术栈

\- React 18

\- TypeScript 5



\## 代码规范

\- 使用单引号

\- 缩进2空格

\- 函数式组件优先

\- 禁止any类型

\- 组件保持单一职责

\- 优先拆分可复用组件

\- 自定义Hooks放在 `hooks/` 目录

\- 工具函数放在 `utils/` 目录

\- API调用统一放在 `services/` 目录

\- 禁止在组件内直接写复杂业务逻辑

\- 命名清晰，避免无意义缩写

\- 样式方案保持项目现有风格，避免混用

\- 公共类型统一管理，避免重复定义相同接口

\- 不要绕过封装直接在页面里写裸请求



\## 项目结构

src/

├── components/ # React组件

├── hooks/    # 自定义Hooks

├── services/  # API调用

├── utils/    # 工具函数

├── types/    # 类型定义

└── pages/    # 页面组件



\## 开发要求

\- 优先保证组件可读性和可维护性

\- 状态尽量靠近使用位置

\- 副作用逻辑统一放在合适的Hook中处理

\- 表单、列表、弹窗等常用模块优先复用已有实现

\- 异步请求必须处理加载态、错误态、空态

\- 关键交互必须考虑边界情况

\- 列表页面默认处理分页、筛选、刷新

\- 表单提交必须处理重复提交

\- 删除、下线、覆盖类操作必须二次确认

\- 禁止写无类型约束的公共方法



\## API协作规范

\- 所有请求统一通过 `services/` 发起

\- 不要在组件内拼接接口路径

\- 请求参数、响应数据必须定义明确类型

\- 前端字段命名必须与接口文档保持一致

\- 接口变更前先确认影响范围

\- 接口失败时统一提示，禁止静默失败

\- 对业务错误码做显式处理，不只判断 HTTP 状态码

\- 分页接口统一使用项目约定字段，不自行发明结构



\## 统一响应处理要求

\- 前端默认按统一响应结构解析

\- 成功响应先判断 `code` 是否为约定成功值

\- 失败响应展示可读错误信息

\- 对未登录、无权限、参数错误、服务异常做统一处理

\- 不在页面中重复实现通用错误处理逻辑



\## 测试要求

\- 新功能必须有单元测试

\- 运行 `npm test` 验证

\- 覆盖率不低于80%

\- 核心交互、状态切换、工具函数必须覆盖测试

\- 修复 bug 时必须补对应测试



\## Docker协作要求

\- 前端构建产物必须可通过 Docker 构建

\- Dockerfile 保持分层清晰，优先多阶段构建

\- 不要把本地开发配置硬编码进镜像

\- 不要把 `.env` 文件打进镜像

\- 静态资源服务配置要和部署环境保持一致

\- 修改构建命令、端口、代理规则时说明影响范围



\## 禁止事项

\- 不要修改 `.env` 文件

\- 不要直接操作数据库

\- 不要删除 `node_modules`

\- 不要在组件中直接调用后端地址常量绕过服务层

\- 不要提交临时调试代码

\- 不要忽略测试失败直接交付



\## 沟通风格

\- 简洁，不废话

\- 遇到不确定的先问

\- 给出方案时说明取舍

\- 修改前说明影响范围